package utils;

import mappers.VariableMapper;
import readers.FileReader;

public class VariableAssign {
    public static void updateExistingVariables(String[] variablesToUpdate) {
//  FileReader.xmlVariables
        for (int varIndex = 0; varIndex < variablesToUpdate.length; varIndex++) {


         String varPathFromXML = FileReader.xmlVariables.get(varIndex).node;
            if ()
        }

    }
}

